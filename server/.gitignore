node_modules/
# Downloaded external binaries (yt-dlp, ffmpeg, ffprobe, deno)
bin/
# Temporary extracted audio files
tmp/
# Yarn internal state
.yarn/
# Angular dist copied here for packaging — built by CI, not committed
app/
# Package build output
dist/
# electron-builder output (installers, win-unpacked) — large, not committed
dist-electron/
# PnP artefacts (replaced by node-modules linker)
.pnp.cjs
.pnp.loader.mjs
